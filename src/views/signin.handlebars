{{! signin.handlebars
    Template for the sign-in page.
    Express route: /signin
}}

<div class="card card--home card--signin">
    <div class="card__vertical-list">
        <img src="/img/logo-dark.png" class="card__logo card__logo--small">
        <nav class="register__component card__horizontal-list">
            <a class="button button" href="/">
                <span class="material-symbols-outlined">home</span>
                <ruby>Home<rt>ホーム</rt></ruby>
            </a>
            <a class="button button" href="/register">
                <span class="material-symbols-outlined">how_to_reg</span>
                <ruby>Register<rt>レジスター</rt></ruby>
            </a>
        </nav>
    </div>
    <section class="register">
        <h1 class="register__heading">Sign in</h1>
        <form class="register__form" method="POST" id="signinform" action="/signin">
            {{> field id="name" label="Username: " icon="person" type="text" placeholder="Enter your name"}}
            {{> field id="password" label="Password: " icon="lock" type="password" placeholder="Enter your password" password="true"}}

            <div class="register__input">
                <input type="checkbox" name="remember">
                <label for="remember">Remember me</label>
            </div>
            <button type="submit" class="button">
                <span class="material-symbols-outlined">login</span>
                <ruby>Sign in<rt>サインイン</rt></ruby>
            </button>
        </form>
        <div class="errormsg hidden"></div>
    </section>
</div>

{{! i do not know where to put form.js, rn it's in public }}

<script src="/js/form.js"></script>